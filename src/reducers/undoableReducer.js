const initialState = {
  past: [],
  present: [],
  future: [],
}
const expression = null
export const undoable = (state = initialState, {type, action} = {}) => {
  switch (type) {
    case expression:


    default:
      return state

  }
}
