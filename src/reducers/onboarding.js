

const initialState = {
  stepIndex: 0,
  listCount: 0,
  ProceedWarning: false
}

export default function onboarding(state = initialState, {type, payload} = {}) {
  switch (type) {
    case expression:

      break;
    default:

  }
}
